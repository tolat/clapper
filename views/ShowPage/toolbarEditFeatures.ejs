<div class="dropdown">
    <button class="btn btn-secondary dropdown-toggle grid-toolbar-dropdown-button" type="button"
        id="dropdownMenuButton2" data-bs-toggle="dropdown" aria-expanded="false" style="width: 4rem;">
        Edit
    </button>
    <ul class="dropdown-menu grid-toolbar-dropdown" id="edit-dropdown" aria-labelledby="dropdownMenuButton2">
        <li><a class="dropdown-item" onclick="selectAll()">Select all</a></li>
        <div class="dropdown-item">Group by column: <br><input class="edit-dropdown-input" id="group-by-input"
                placeholder="E.g. 'Department'">
        </div>
        <div class="dropdown-item" class="edit-dropdown-input" style="display: none;" onclick="groupBy(null)"
            id="cancel-grouping">
            Clear grouping</div>
        <div class="dropdown-item">Add rows: <br><input id="add-rows-input" class="edit-dropdown-input"
                onkeydown="validateModalInput(event, 'number')" placeholder="Add rows at end of grid"></div>
        <div class="dropdown-item">Auto-number by column:<br><input id="auto-number-input" class="edit-dropdown-input"
                placeholder="E.g. 'Department'"></div>
        <div class="dropdown-item"># of frozen columns: <br><input id="frozen-columns-input" class="edit-dropdown-input"
                placeholder="Freeze rows at left of grid" onkeydown="validateModalInput(event, 'number')"></div>
        <div class="dropdown-item" onclick="toggleAutoFillRangeModal(true)">Auto-fill range</div>
    </ul>
</div>